export interface CardData {
  id: string;
  field: string;
  description: string;
  link: string;
  field_mindmap_path: string;
}

export interface FieldMenuItem {
  title: string;
  subtitle?: string;
}